export const CARD_TYPES = {
    LOGIN_CARD: "Auth",
    FORGOT_PASSWORD: "ForgotPassword",
    REGISTER: "Register"
}

export interface player {
    position: string;
    ID: number;
    name: string;
    price: number;
    team_name: string;
}